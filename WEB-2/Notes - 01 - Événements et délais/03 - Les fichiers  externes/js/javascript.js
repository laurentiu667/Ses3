// Lors que la page sera chargee , execute l arrow fonction

// window.onload - () = {...}
window.addEventListener("load", () => {
    let para = document.querySelector("#paragraphe")

    para.style.color = "green";
})
